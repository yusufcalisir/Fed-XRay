# Fed-XRay Dependencies
# Federated Learning Medical Diagnosis Application & CDSS API

# Core ML Framework
torch>=2.0.0
torchvision>=0.15.0

# API Backend (FastAPI & ASGI Server)
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Scientific Computing
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Image Processing
Pillow>=10.0.0
matplotlib>=3.7.0

# PDF Report Generation (fpdf2 — maintained fork of fpdf, used by report.py)
fpdf2>=2.7.0

# Clinical Voice Briefing (gTTS — internet-based TTS)
gTTS>=2.3.0

# Homomorphic Encryption (CKKS via TenSEAL — optional, gracefully skipped if unavailable)
tenseal>=0.3.14

# Privacy & Cryptography
cryptography>=41.0.0

# Async HTTP utilities
httpx>=0.27.0
aiofiles>=23.0.0
